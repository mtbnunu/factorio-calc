import { ref, computed ,watch} from "vue";


export const beltrate = ref(45);
export const hideSmelting = ref(true);
export const hideFluid = ref(true);

export const speedMultiplier = ref(1);
export const productivityMultiplier = ref(1);